# Nothing is required to run the tests. For the live demo:
ANTHROPIC_API_KEY=            # extraction; model: claude-sonnet-5 (~$0.60-1.00 per 150-page spec)

# Value receipt
BASELINE_HRS_PER_SPEC=6       # estimator's own measured hours per spec — time it in week one
